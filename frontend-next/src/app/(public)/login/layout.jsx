export const metadata = {
    title: "Sign In",
    description: "Sign in to your Mazel Tote account to track orders and manage your profile.",
};

export default function LoginLayout({ children }) {
    return children;
}
