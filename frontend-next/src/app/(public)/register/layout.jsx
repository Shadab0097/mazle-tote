export const metadata = {
    title: "Create Account",
    description: "Create your Mazel Tote account to start shopping and supporting charities fighting antisemitism.",
};

export default function RegisterLayout({ children }) {
    return children;
}
