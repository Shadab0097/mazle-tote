export const metadata = {
    title: "Shopping Cart",
    description: "Review the items in your Mazel Tote shopping cart before checkout.",
};

export default function CartLayout({ children }) {
    return children;
}
