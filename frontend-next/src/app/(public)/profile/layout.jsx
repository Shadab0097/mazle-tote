export const metadata = {
    title: "Profile",
    description: "Manage your Mazel Tote account profile and settings.",
};

export default function ProfileLayout({ children }) {
    return children;
}
