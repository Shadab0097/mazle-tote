export const metadata = {
    title: "Checkout",
    description: "Complete your Mazel Tote purchase and choose a charity to support with your order.",
};

export default function CheckoutLayout({ children }) {
    return children;
}
